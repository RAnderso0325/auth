<% if(alerts.error){%>
	<% alerts.error.forEach(function(msg){ %>
		<div class="toast">
			<%= msg %>
		</div>
	<%});%>
<%}%>
<% if(alerts.success){%>
	<% alerts.success.forEach(function(msg){ %>
		<div class="toast">
			<%= msg %>
		</div>
	<%});%>
<%}%>